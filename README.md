


```markdown
## 🧪 Качество и тестирование

Проект разрабатывается с упором на тестируемость и предсказуемость результатов. Покрытие тестами бизнес‑логики составляет **≥ 80 %**, что подтверждает надёжность ключевых функций.

### Покрытие по модулям

| Модуль | Назначение | Покрытие |
|--------|------------|----------|
| `src/reports` | Агрегация транзакций, формирование отчётов (топ‑7, «Остальное») | ~69 % |
| `src/services` | Логика операций: кэшбек, инвестиции, поиск транзакций | ~98 % |
| `src/utils` | Вспомогательные функции: загрузка CSV/Excel, нормализация данных, загрузка настроек | ~83 % |
| `src/views` | Формирование дашборда и структуры ответа для фронтенда | ~88 % |
| `src/main` | Точка входа (CLI) | 0 % |

> Покрытие `src/main.py = 0 %` — это ожидаемое поведение: точка входа не требует отдельного покрытия, так как вся логика уже протестирована на уровне функций, которые она вызывает.

### Как проверить покрытие

1. **Терминальный отчёт:**
   ```bash
   pytest --cov=src --cov-report=term
